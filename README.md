# 拾猫记

《拾猫记》是一个以猫咪陪伴为主题的静态前端 App 原型。用户通过专注阅读、待办、笔记、闹钟等功能获得代币，再购买道具和猫咪互动。

## 运行方式

直接打开 `index.html`，或使用任意静态服务器预览：

```bash
python -m http.server 4173
```

本地数据保存在浏览器 `localStorage` 的 `mi_state` 中。

## 调试入口

普通访问会隐藏开发者重置入口。需要重置演示数据时，在地址后加：

```text
?debug=1
```
